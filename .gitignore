# Credenciais
.env
.env.local

# Build artifacts (template fin40 default — Astro overrides com '!' abaixo)
data-extras/
data.js
data-extras.js
# Astro: app.bundle.js + data/* + astro-data.js precisam ir no build Docker
# (geramos local com node build-jsx.cjs + python build_astro_data.py antes do push)

# Reports cacheados (gerados por write-reports.cjs do cliente, ou por API on-the-fly)
report.json
report-*.json

# Mapping anonimo->original — NUNCA commitar (vaza des-anonimização)
raw-data/_anon_mapping.json
__pycache__/
*.pyc

# bi.config.js é cliente-específico mas NÃO tem secrets — é tracked pra cada repo cliente.
# Mantém aqui só pra referência (descomente se quiser experimentar local sem commitar).
# bi.config.js

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
*.log

# Editor
.vscode/
.idea/

# Não ignora QUICK_START.pdf — ele é distribuído junto pra time não-dev
# QUICK_START.pdf
